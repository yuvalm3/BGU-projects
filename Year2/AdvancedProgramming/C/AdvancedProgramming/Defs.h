#ifndef DEFS_H
#define DEFS_H

// False and True status
typedef enum e_bool {
    false, true
} bool ;

// Success and Failure status
typedef enum e_status {
    success, failure
} status;
#endif //DEFS_H
